Scala

```
val arr = Array(1, 2, 3)

val listBuffer = ListBuffer.empty[String]
listBuffer += "one"
listBuffer += "two"

val stack = Stack.empty[String]
stack.push("test")
stack.pop()

```

```
List
Array

ListBuffer.empty[T]
ArrayBuffer.empty[T]
```

